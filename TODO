Migrate away from cpx; use a module bundler like Parcel or webpack
Fix "active" style on nav menu
Add MQTT.js (or similar) and subscribe to ajbkr/feeds/flame-detector
  Add public-facing dashboard?
Restructure things.json so that "category" values become keys?
  {
    "decades": [{
      "name": "AngularJS",
      "url": "https://angularjs.org/"
    }, {
      // ...
    }],
    "home": [{
      "name": "Arduino",
      "url": "https://www.arduino.cc/"
    }, {
      // ...
    }],
    "work": [{
      "name": "Backbone.js",
      "url": "https://backbonejs.org/"
    }, {
      // ...
    }]
  }
Unit tests for checkedThings (helpers/check-things.js?)
Add links to relevant content elsewhere on the Internet

+Remove Milligram (replace with Bootstrap)
+Improve getNavigation() unit tests
+Retrieve navigation from navigation.json file
+Split routes into separate files and serve "/" ASAP (defer "/about")
+Enforce sort order for navigation (Map maybe?)
+Cache checkLinks() results
+Replace jasmine-node with jasmine (or some other unit testing framework)
+Move getNavigation() into its own file (get-navigation.js) and add unit tests for it
+BEMify header
+Restore LinkedIn profile
+Add LinkedOut profile
+Add README
+Ensure nsp passes
+Add Google Analytics
+Improve mobile experience (layout, etc.)
+Update clean-css to 4.x
+Replace GitHub, LinkedIn, and Twitter links with social icons
